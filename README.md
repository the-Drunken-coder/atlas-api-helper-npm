# atlas-api-helper-npm

Standalone repo for the npm package `@atlasnpm/atlas-api-helper`.

- Source of truth lives in the monorepo: `the-Drunken-coder/ATLAS`.
- The `package/` directory is mirrored from the monorepo by a manual GitHub Action.
- This repo is intended to own publishing workflows + `CHANGELOG.md`.
